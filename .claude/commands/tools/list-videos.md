---
description: List all deployed video URLs
allowed-tools: Bash(python:*)
model: haiku
---

List all deployed video URLs.

Run this Python script:

```bash
python -m scripts.controllers.list_videos_controller
```

Display the results to the user as it is.
